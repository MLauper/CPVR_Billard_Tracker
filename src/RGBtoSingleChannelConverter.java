import ij.process.ImageProcessor;

public class RGBtoSingleChannelConverter extends Image {
    public RGBtoSingleChannelConverter(ImageProcessor imageProcessor) {
        super(imageProcessor);
    }
}
